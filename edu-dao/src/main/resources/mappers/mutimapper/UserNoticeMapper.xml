<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.yunzhi.edu.dao.muti.UserNoticeMutiMapper">

    <!-- 
    <resultMap id="userMap" type="com.yunzhi.edu.entity.User" >
        <result column="user_id" property="userId"/>
        <result column="org_code" property="orgCode" jdbcType="VARCHAR" />
        <result column="staff_num" property="staffNum"/>
        <result column="role_name" property="roleName"/>
    </resultMap>
    
    <resultMap id="userNoticeMap" type="com.yunzhi.edu.entity.UserNotice">
        <result column="user_id" property="userId"/>
        <result column="notice_id" property="noticeId"/>
        <result column="is_read" property="isRead"/>
    </resultMap>
    
    <select id="listUserForNotice" resultMap="userNoticeMap">
        select user_id 
        from edu_user u , 
        (select staff_num from school_student
        <where>
            org_code = #{orgCode}
            <if test="classId != null">
                and class_id = #{classId}
            </if>
            <if test="departmentId != null ">
                and department_id = #{departmentId}
            </if>
              and is_activate = 1
        </where>) s
        where u.staff_num = s.staff_num
    </select>
     -->
</mapper>